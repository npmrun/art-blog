---
import SitePage from "@blog/Layouts/Base/SitePage.astro";

export interface Props {
    title: string;
    desc: string;
    name?: string;
}

const { title, desc, name} = Astro.props as Props;
---

<SitePage title={title} desc={desc} black name={name}>
    <slot />
</SitePage>
