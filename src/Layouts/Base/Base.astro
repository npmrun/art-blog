---
import BaseHead from "@blog/components/BaseHead.astro";
import { siteInfo } from "@blog/config";

export interface Props {
  title?: string;
  desc?: string;
  icon?: string;
  name?: string;
}

const { title, desc, icon, name } = Astro.props as Props;
---

<!DOCTYPE html>
<html lang={siteInfo.lang} class="h-full">
  <head>
    <BaseHead
      title={title || siteInfo.siteName}
      desc={desc || siteInfo.siteDesc}
      icon={icon || siteInfo.icon}
    />
    <slot name="head" />
    <script src="/theme-change.js" is:inline></script>
</head>
<!-- dark:brightness-[.65] -->
<body class:list={['bg-slate-100 h-full md:!overflow-auto',name]}>
    <slot />
</body>
</html>
